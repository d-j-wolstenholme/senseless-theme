# Next session — Senseless (Canon v2.19)

Read `CLAUDE.md` → run `scripts/reconcile.sh` → read the Project Instance + State Surface first.
**Machine last used:** MacBook Pro — confirmed by Daniel 17 Jul. NOTE: hostname can display as `Daniels-MBP.Home` (network-dependent); canon's `Daniels-MacBook-Pro.local` is the same machine.

## Done last session (2026-07-17, Claude Code · Fable) — /pages/delete-account + Privacy Policy cross-links
**Google Play data-safety URL** (spec: `../senseless-app/docs/delete-account-page-spec.md`; Daniel signed off page copy + all 4 privacy edits in-session).
- **NEW 7th policy page `/pages/delete-account`** — page gid `712778711388`, shared `policy` template, body empty, content in `policy.*` metafields (prose via the RT converter, `faq: []` like rewards-terms), + `global.title_tag`/`description_tag` + `seo.hidden=1` like siblings; handle added to `ss_noindex_handles` (`layout/theme.liquid`) and deployed via `deploy.sh` (theme-check 0 errors; reviews-guard post-deploy 5/5; Asset-API remote==local). Copy mirrors the app's shipped confirmation dialog (`SettingsView.swift`); compliance-check PASS.
- **Privacy Policy updated in place:** "Your rights" self-serve deletion sentence, "How do I request deletion?" FAQ answer rewritten, "Delete your account" appended to see-also, `last_updated` → 2026-07-17. Verified live: 200, 3 links → `/pages/delete-account`, unchanged sections intact (six-years retention, ICO, DPA 2018).
- **Canon:** `scripts/policy-metafields.py` updated to 7 pages — the MCP-pushed rich-text values were generated by the script's own converter, so script == live (pre-edit parity was verified first).
- **Wrong-store record corrected:** the 16 Jul "page exists but 404s / needs bespoke template" story was the Totally Numb mix-up. This store had no such page until today (29-page list, `hasNextPage:false`, checked before building).
- **Full sitemap audit (same session, Daniel's ask):** all four child sitemaps cross-referenced against live robots state + admin `seo.hidden` flags. Pages 19/19 expected (delete-account + all 9 noindex handles correctly excluded; 2 unpublished absent) · collections 16 curated (`/collections/all` correctly excluded + noindexed) · products 16 + home (matches admin) · blogs index + 5 articles. `seo.hidden=1` ↔ `ss_noindex_handles` are exactly 1:1. **No sitemap changes needed.** One conflict found and investigated: `/blogs/guides` is sitemap-listed while noindexed (9-Jul Fix A) — tested blog-level `seo.hidden=1` live: it removed the ARTICLES from the blogs sitemap too (cascade), reverted within minutes, sitemap verified restored 6/6. Verdict: structural on Shopify — the GSC "submitted URL marked noindex" warning on the blog index is **accepted**, articles keep their sitemap entries.

### Next / watch (this task)
- **Daniel:** paste `https://senseless.uk/pages/delete-account` into the Play Console **Data safety** form (the senseless-app session guides that step; both stores' forms must say deletion is provided).
- **Flagged, not built:** `mobile-app-privacy-policy` is also missing here (same wrong-store casualty). Store listings need a privacy-policy URL — likely the main `/pages/privacy-policy` serves; needs a decision.
- **Resolved in-session:** delete-account now renders BOTH robots tags (theme `noindex,follow` + seo.hidden `noindex,nofollow`) — settled state, matches siblings. Pre-existing oddity (untouched): `llms-txt` renders only the seo.hidden tag. Transient: `/blogs/guides` may show a cached `noindex,nofollow` from the 17-Jul seo.hidden test window — self-heals, don't re-raise.
- **If totally-numb.com recovers** (503 on 17 Jul): check Totally Numb for the stray 16-Jul `delete-account`/`mobile-app-privacy-policy` pages and delete them — they name the Senseless app.
- Optional (needs decision + deploy): the hardcoded footer legal bar doesn't link delete-account — not required for Play; privacy-policy links to it 3 ways.

## Carried forward — open flags (compressed from 9–12 Jul sessions; detail in Session Log)
- **MHG footer logo** (12 Jul, commit `508d399`): Daniel to sign off full-lockup vs monogram. Lockup is a light-background asset — won't read if the footer band ever flips to `ink`.
- **Injectables** (10 Jul, `1fa8678`): botox fold-vs-keep (301-merge call, Daniel, deferred); GSC watch — `numbing-cream-for-injections` should move off 0 impressions.
- **Cannibalisation** (9 Jul, `fd50656`/`3e95454`/`065052c`): GSC watch — strongest absorbs "best numbing cream" via 301; next SEO fix = spray/gel format-cluster splits (audit ranks 2–3). `/pages/articles` scope = education cluster (Daniel can widen).
- **Live theme carries TWO inert orphans** (scoped deploys can't delete remote files; Asset-API delete barred): `blocks/footer-copyright.liquid` + `templates/page.how-long-numbing-cream-takes-to-work.json` (left by the takes-to-work merge `64fcfdc`; page unpublished, template unreferenced). Full remote-vs-local inventory 17 Jul: every git-tracked theme file present on live (560/560) — remote = 560 + these 2; nothing ever removed. Clears on a future full re-sync; harmless.

## Carried forward — open from 2026-07-04 rewards session (still open)
- **⚠️ Coming-soon earn methods on `/pages/rewards` are Daniel's explicit call — do NOT revert as a compliance regression** (framed "coming soon", not claimed active). Only birthday has a decided value (100pt); referral/reviews/social have NO decided values — don't invent numbers.
- **4 planned earn methods NOT built** (App project): birthday (`39358bc3-75ea-81d5`, 100pt, needs DOB capture) · referral (`39358bc3-75ea-81a0`, value undecided) · social (`39358bc3-75ea-81c0`, blocked — accounts don't exist) · reviews (no direction). When any ship: flip `coming_soon` off + set real value.
- **Rewards T&C legal review (MHG/legal)** — §6–8 placeholder numbering flagged; live per Daniel; Status Built (not Final) for this reason.
- **Rewards link-out broken** (3 Jul): hosted account page shows no balance — Customer Account UI Extension may not be installed on `senseless-numbing`. Handed to App project.
- **Store province "England"** (should be Lancashire) — UNFIXABLE via UI/API; never rendered.
- Header mis-links: FIXED 10 Jul (`1fa8678`) — Application-guides menu now points at the dedicated `how-long-*` pages.

## Carried-forward gotchas (still valid)
- **Store gate recurring:** the Shopify MCP connector can silently point at **Totally Numb** — gate every session on `shop { myshopifyDomain }` = `senseless-numbing.myshopify.com` (display name/domain look right on BOTH stores). Reliable CLI path: `scripts/refresh-token.sh` + `deploy.sh` (both store-pinned).
- **`senseless-numbing.myshopify.com` 301s to `senseless.uk`** — a bare-origin curl greps a redirect stub (false MISS); always `-L`. The senseless.uk edge normalises `?cb=` (no cache-bust); renders can flap between cached snapshots after admin-side changes — theme-FILE deploys bust page caches, `metafieldsSet` does not (verify stored value via Admin API; render self-heals).
- **Adding a policy page:** `pageCreate` (handle + `templateSuffix: policy` + `isPublished`) → `policy.*` metafields via the `RT` converter in `scripts/policy-metafields.py` (never hand-JSON) → `global.title_tag`/`description_tag` + `seo.hidden=1` → add handle to `ss_noindex_handles` in `layout/theme.liquid` (+ deploy). Script gotchas: module-level `SHOPIFY_ACCESS_TOKEN` read — set a dummy for offline RT generation; never run wholesale (per-page `last_updated` values regress).
- **Deploy order: SECTION (schema) BEFORE the TEMPLATE using new schema settings** — a combined push makes Shopify validate the template against the old schema and silently STRIP unknown block-settings. Verify block settings via Asset API after.
- **Footer tiers:** 4 columns (Shop/Learn/Help/Company) are Shopify nav menus (`senseless-footer-*`, `menuUpdate`, no deploy); the bottom **legal bar** (Privacy/Terms/Cookie/Rewards Terms + MHG logo) is **hardcoded** in `sections/senseless-footer.liquid`.
- **Never set `seo.hidden` on a BLOG to tidy the sitemap** — it cascades: the blog's ARTICLES drop out of the blogs sitemap too (tested live 17 Jul, reverted). The `/blogs/guides` sitemap-vs-noindex conflict is structural; accept the GSC warning. Child sitemaps need their `?from=&to=` params (from the sitemap index) — a bare fetch returns empty (false "missing").
- **Retiring a page via 301 needs an UNPUBLISH** (redirect is inert while the page resolves 200); create redirect first, then unpublish.
- **Intermittent 503s under rapid curls** can falsely trip `deploy.sh`'s post-deploy reviews-guard — space out re-checks before treating a guard-fail as real.
- `.env` Admin token expires — `./scripts/refresh-token.sh` then `set -a; source .env; set +a`. No rollback theme.
