# Senseless — Decisions Log (Local Mirror)

**Mirrors the active volume of the Notion Decisions Log.**
Active volume: Vol 1 — May 2026
Notion source: https://www.notion.so/36d58bc375ea81708d1ac0fe0724d445

Newest at top. ISO 8601 timestamps in BST.

When this file is older than 24 hours, run `/drift-check` to surface any drift between this local mirror and the canonical Notion log.

---

### 2026-06-26 BST (Image rollout from boss's Drive: About/System/articles/guides/Trade + contact email; founder-accepted BRAND.md/compliance exceptions)
**Decision:** Replaced the 2026-06-25 first image-placement (which used stale "NUMBING"-era `.jpg` packaging + wrong-fit product photos in editorial/nav slots) with the boss's real current-design images from the shared Drive folder "Senseless generic images". **Placed:** About (hero/MHG/specialism/honesty/voice/practice), System hero, 5 article featured images (via admin `articleUpdate`), 3 guide-page heroes (does-it-hurt/microneedling/laser at 16:9), Trade (hero + offer trio + fit band + position/MHG mark). Non-product/editorial/wayfinding slots use a **contained brand-asterisk** (new global CSS in `senseless-typography` so the mark isn't `object-fit:cover` cropped); genuine product slots use current `.webp`. **Ratio controls added** to `senseless-guide-hero` (`image_ratio`) + `senseless-trio-card-row` (`media_ratio`, CSS var: 1:1/5:4/4:3/3:2/16:9) and `articles-hub` → 16:9, per founder direction "accommodate the slot to the image, don't crop". **Reviews-guard marker corrected** (`jdgm-review-snippet-widget` → `jdgm-preview-badge` — old assertion was a false-fail). **Contact:** Media & partnerships email → `info@matrixhealthgroup.co.uk` (customer + data/legal stay `cs@senseless.uk`; data/legal mailbox name pending). **Founder-accepted BRAND.md exception:** clinic/procedure imagery incl. needle injections now appears on guide + trade pages — overrides BRAND.md "no clinical/medical setting imagery on consumer pages"; behind storefront password. **Compliance flags routed to MHG/legal before public go-live:** (1) the **"Feel Less. Experience More." tagline** baked into several Drive images reads as a reduced-sensation/anaesthetic claim; (2) **`Reviews.png` "5000+ verified reviews / 4.92"** is false vs the real 231 / 4.88 / not-verified-buyer. **Asset-version rule:** `.jpg` stills = OLD discontinued design (spray mislabelled 50ml, "NUMBING" naming); `.webp` heroes + per-SKU = CURRENT — do not reuse the old `.jpg`. **Open (Judge.me-side, NOT theme):** the all-reviews page widget shows 0 despite 231 genuine reviews — `page.reviews.json` was never deployed and Judge.me's loader renders live; needs the Judge.me dashboard. **Mechanics:** all on `main`, deployed via `scripts/deploy.sh` (reviews-guard post-verify green each push), theme-check 0. Commits `db0867f → 44a2d49 → 36840f5 → 2b60a29 → 00d53ee → 5f19ec1 → c1792b8`. Build report mirrored to Notion (2026-06-26 sub-page).

### 2026-06-24 12:07 BST (Homepage — reorder + new trust bar + FIRST live practitioner testimonials; deployed live)
**Decision:** Shipped a homepage "proof then products" pass to the live theme (`#199324434780`), built on a local `develop` branch then merged to `main`. **(1) Reorder** (Peter): consumer stack now `hero → trust bar → comfort-by-procedure → practitioner cards → strength tiers → remainder`; the **Trade & wholesale** band (`index.json` key `practitioners`, type `senseless-image-text-band`) was **kept intact and moved lower** (not adjacent to the new cards). Procedure + tier block bodies untouched (reorder only). **(2) Trust bar** (`senseless-trust-bar.liquid`): rebuilt to a **rating-led** strip — genuine Judge.me **shop-wide average 4.88 → shown "4.9 / 5"** (computed weighted across the 231-review corpus; NOT hardcoded 5), **"230+ reviews"** grouped into the rating cluster, plus ticks UK Formulated · CPSR Assessed · Trusted by UK Practitioners · Free delivery over £40. **(3) Practitioner cards** — NEW removable section `senseless-practitioner-cards.liquid` (new `index.json` key `practitionercards`, NOT the trade `practitioners` key): **the first genuine practitioner testimonials to go live** — Ashley · Kent, Libby · Bristol, Lucy · Manchester — inserted **verbatim** (emoji + original spelling preserved, no rewording). **(4) Brand-led restyle of both** new sections as a family: `#6B3FA0 → #984AE8` purple→violet accent (echoing the asterisk mark) — gradient star fill + card top-accent; eyebrow "Practitioner voices"; solid brand-purple monograms; quote-led cards with attribution pinned to the foot (equal-height grid). **Consent / compliance:** founder confirmed 23 Jun 2026 the three reviews are genuine and **consent-to-publish + the live-push gates are cleared**; published per explicit founder go. **Standing compliance flag (founder-accepted for live):** Card 2 (Libby) is live verbatim incl. *"all tested and registered"* (could read as medicinal/MHRA registration) and competitor ref *"TKTX… so many fakes… the real deal"* — left unedited per instruction; flagged for a possible later copy softening (not structural). **Pending-before-public (recorded in section comments, not on page):** practitioner substantiation; "Free delivery over £40" threshold live in checkout; review-count scope; note reviews are **not verified-buyer** (0/54 sampled) so label is "reviews", not "verified". **Mechanics:** theme-check 0; deployed `--allow-live`; `main` commits `b743a1b → ddb2ab9 → 528bcf7 → 9402b21`, pushed origin. **Tidy-up:** local `develop` branch deleted (merged); throwaway preview theme `#200345092444` deleted. Mirror to Notion.

### 2026-06-16 14:54 BST (Compliance — A5 final amend, on top of b57c49d; pushed to main)
**Decision:** Final founder amendments to the A5 repair. (1) **P9 format-comparison FAQs** (numbing-gel/spray) re-worded to drop the "most concentrated format" line — strength is **tier-based** (Clinical/Advanced/Professional, every format in all three), format chosen by **use-case** ("Strength isn't set by the format — it's the tier you choose… Pick the format for the area…"). No potency/speed/duration/efficacy claim. (2) **Residuals fixed** (previously flagged): `collection.numbing-cream-for-lip-fillers.json:23` "eased by the numbing most practitioners use" → effect assertion removed, practice only ("Most practitioners apply numbing cream as part of prep."); `:36` "numb the lips themselves" → "apply numbing cream themselves." (3) **does-it-hurt q1**: "you may still feel pressure or movement" → "you may feel pressure or movement." Body copy only; "numbing" as category noun only; no slug/handle/anchor/nav/H1/meta/canonical/schema. **Status:** committed on top of b57c49d and **pushed to `main`** (deploys to live theme) per founder sign-off. Mirror to Notion.

### 2026-06-16 14:26 BST (Compliance — A5 effect-use repair; founder Content Standards supersedes the 12 June "leave for legal" hold)
**Decision:** Founder-governed Content Standards now **supersede** the 2026-06-12 "routed to MHG/legal, not changed" hold on live *effect-use* of "numbing". **Governing rule:** remove every claim about what numbing does to sensation/discomfort — **including negations** ("doesn't remove all sensation" still references sensation → out) — and replace with **observational/preparation language**. Register split: product + commercial collections → observational **comfort** framing ("supports comfort"); educational / FAQ / pain guides + llms.txt → **"a cosmetic preparation, not an anaesthetic"** framing (carries the honest expectation without a sensation claim). The compliant "**not an anaesthetic/anaesthesia**" negations (`page.does-numbing-cream-work`) were already clean and left as-is. **Applied this session (body copy only; no slug/handle/anchor/nav/H1/meta/canonical/schema touched):** ~40 lines across 24 files — P1+P2 sensation/discomfort effect-use (incl. the one hardcoded Liquid line `sections/senseless-comfort-compare.liquid:22`, rendered on does-laser/does-microneedling), P1b "arriving already numbed" → "arriving prepared", P1c "numb the skin themselves" → "apply numbing cream themselves", P5 colloquial "treats every situation" → "handles". **Addendum (same session, superseding the audit's "out-of-scope"):** P7 application-timing reframed to customer-voice on the 3 cream PDPs + collection.numbing-cream + using-numbing-cream ("Many customers tell us they get their best results applying around 45–60 minutes before…, though this varies"); P9 format-comparison FAQs (numbing-gel/spray) replaced with corrected customer-framed answers ("the cream is our most concentrated format… match the format to the area"). **"numbing" retained only as a category noun throughout.** **Held / not touched:** any "broken/unbroken skin" wording (pending separate check); application-timing was previously flagged out-of-scope but is now done. **Flagged residuals (not in the fix list, surfaced during repair — await ruling):** `collection.numbing-cream-for-lip-fillers.json:23` ("eased by the numbing most practitioners use") and `:36` ("most practitioners numb the lips themselves") — effect/verb uses the pattern sweep didn't catch. **Status:** committed locally on `main`, **NOT pushed** — copy held for founder review before it deploys to the live theme. JSON validity confirmed on all 22 edited templates; theme-check clean on the 2 Liquid files. Mirror this entry to the Notion Decisions Log.

### 2026-06-15 06:13 BST (Canonical — "Comfort" product brand name vs "Numbing" SEO category descriptor)
**Decision:** Locked the Comfort-vs-Numbing naming rule. **"Comfort [format]"** (Comfort Cream / Comfort Gel / Comfort Spray) = the Senseless **product brand name** — used wherever a specific Senseless product is named (titles, product descriptions, brand copy, product image alt text, cross-sell/cart copy naming an item, JSON-LD product name). **"Numbing [format]"** = **SEO category descriptor** — used only where a generic category noun / search term is intended (collection titles + meta, meta titles/descriptions capturing search intent, nav By-Format/collection labels, educational guide + article body copy, FAQ category questions). Test: does the copy claim/imply the Senseless product numbs the user? Yes → Comfort; describes a search category → Numbing permitted. Ties into the standing COMPLIANCE.md "numbing" effect-claim ban (avoids a medicinal-efficacy claim / MHRA reclassification). **Docs updated same session:** COMPLIANCE.md (new "Product naming — UK cosmetics compliance" sub-rule), BRAND.md (Brand → Product naming).
**Audit basis:** Phase 1 read-only audit (theme repo + live Admin) classified 290 hits — CHANGE 12 · KEEP 247 · GREY 31. Key finding: the store never used "Numbing [format]" as a product brand name (titles are "[Tier] Strength [Format]"), so almost all hits are legitimate category/SEO (KEEP). Phase 1B SEO-protection layer confirmed all 12 CHANGE hits SEO-safe; the 31 GREY split into **9 SEO-RISK** (8 PDP SEO `title_tag`s + senseless-vs-ametop title — category keyword in the meta title, do NOT replace) + **22 GREY-SEO** — all HELD for owner decision.
**Phase 2 applied (this entry):** the only SEO-cleared CHANGE = the **3 cream PDP product-media alts** "Senseless [Tier] Strength Numbing Cream" → "…Comfort Cream" (productUpdateMedia) + their **9 image-manifest.json** mirror records (3 PDP + 6 tube). Gel/spray live alts already used "Comfort"; JSON-LD name derives from title (already clean). **HELD, not changed:** 8 PDP SEO title_tags + vs-ametop title (SEO-RISK), 6 PDP short-descriptions, 2 bundle kit-contents, 14 manifest range/lifestyle/STATE records (GREY-SEO) — await per-cluster owner ruling. Recommended pattern if Comfort is added to SEO titles = *additive* ("Comfort Cream — Clinical Numbing Cream | Senseless"), keeping the keyword. Build report + SEO layer filed to Notion hub. theme-check 0; no theme files changed (no theme deploy).

### 2026-06-12 06:22 BST (Full-site audit remediation — apply-all, Batches 1–4)
**Decision:** Acted on the 2026-06-12 multi-agent audit (`docs/AUDIT-2026-06-12.md`) with owner-approved "apply-all", in batch order; theme deploy held to session-end. **Batch 1 (wiring):** bound `product` pickers on 25 cross-sell/product cards (live imagery + quick-add); compliance-scrubbed the 8 orphan page templates (all came back publishable — every `£TBC` → live price, 2 timing/duration claims reframed to observational, dead `/blogs/guides/topical-numbing-for-body-art` link fixed in 2 spots); fixed the `senseless-complete-prep` malformed-URL 404 bug. **Batch 2 (SEO/schema):** removed duplicate JSON-LD (`ItemList` from collection-grid on 15 collection hosts, `BreadcrumbList` from articles-hub); aligned the `senseless-org-schema` Organization node to `cs@senseless.uk` + unspaced phone; trimmed homepage meta 208→147 chars; `og:image` http→https + added `twitter:image`; added reciprocal procedure→guide links on 4 collections; swept 89 image alts (deduped generics; category-noun "numbing"→"topical preparation" on all non-format surfaces, kept only on the 3 format collections). **Batch 3 (visual canon):** migrated 36 sections to the canonical `clamp()` type scale; eyebrows → 600/0.2em (0.12em card labels); section padding → 96/56/44 @ 749/989 with stray breakpoints consolidated to 749/990; fixed all neutral/black-border secondary CTAs → purple-outline/purple-fill; strength-matrix muted `#8E8A82` → `#5C5853` (WCAG AA) + 11px caps → 12px. **Card-radius standard set:** cards 4px · buttons/inputs/buy-controls 14px · pills 999px (the theme-majority; BRAND.md's never-built "product-card 0" → 4px). **Batch 4 (motion):** built `snippets/senseless-reveal.liquid` — one reusable IntersectionObserver scroll-reveal (fade + 14px rise, ≤320ms stagger), reduced-motion- and no-JS-safe (hidden state gated on a JS-added `.ss-reveal-on` class under `prefers-reduced-motion: no-preference`), wired once in `theme.liquid`; applied to 10 sections (trio/procedure/format/cross-sell/strength-ladder staggered; image-text-band text→media; key-facts/callout/pull-quote/newsletter single); deliberately excluded heroes, product-hero, collection-grid buy surface, trust-bar, FAQ, header/footer, cookie-consent. theme-check 0 errors throughout. **Docs updated same session:** BRAND.md (card radius), SECTIONS.md (motion + 4 unreferenced sections kept as editor library, not deleted). **NOT touched (holds):** effect-claim "numbing/numbed" copy (MHG/legal); Judge.me review corpus (Peter/legal); 7 photography placeholders. **Flagged, not actioned:** GA4 (needs measurement ID); Dondy WhatsApp widget (an app embed / ScriptTag — not in the theme repo, so consent-gating must be done in the Dondy app config / app-embed toggle, not theme code). **Deferred to this session-end (live admin writes):** assign the `aesthetic-numbing-cream` collection template; create the 8 scrubbed pages (all published); populate missing SEO meta (aesthetic-numbing-cream title, waxing desc, bundles, reviews); EMLA/Ametop retitles.

### 2026-06-12 05:19 BST (Canon correction — product range is 15 SKUs + cleanser + 5 bundles; gel in two sizes; Clinical Spray exists)
**Decision:** A multi-agent full-site audit (2026-06-12, MacBook Pro) confirmed the repo canon docs were stale at the retired **10-SKU / gel-35ml-only / no-Clinical-gel** range, while the live `senseless.uk` store, the built templates, and Notion CANONICAL STATE §1 all carry the current range. Reconciled the docs to live reality (verified via Shopify Admin API): **10 products → 15 single SKUs** — Cream `S10CL S30CL` / `S10AD S30AD` / `S30PR` (Professional 30g-only); Gel in **both 15ml and 35ml** across all three tiers `SG15CL SG35CL` / `SG15AD SG35AD` / `SG15PR SG35PR`; Spray `SSPCL SSPAD SSPPR` (a **Clinical Spray `SSPCL`** now exists, absent from the old list); **Foaming Cleanser `FOAM`**; plus **5 bundles** `SBUN-CL-S/-L`, `SBUN-AD-S/-L`, `SBUN-PR-L` (Clinical/Advanced Starter + Ultimate, Professional Ultimate). Updated `CLAUDE.md` Project Overview + `docs/ARCHITECTURE.md` Products row; fixed 3 stale "15ml"-specific gel alts in `image-manifest.json` to size-agnostic (gel ships both sizes). **No template / section / live-theme changes** — templates already match the store. Supersedes the 2026-05-26 "10 SKUs confirmed" and 2026-05-28 "no Clinical Gel" entries (left in place as history). Docs-only session; no code touched.

### 2026-06-12 05:19 BST (Compliance — category-noun "numbing" exception extended to PDP descriptions + guide/SEO pages)
**Decision:** Owner-approved extension of `COMPLIANCE.md`'s "numbing as a category descriptor" exception. The product-type noun "numbing cream/gel/spray" is now permitted on (a) **product (PDP) short descriptions** and (b) **guide/SEO page body copy whose `<title>` targets the term** — in addition to the existing collection-page + Key Facts + slug/title/meta/nav surfaces. The **Hard Rule is untouched**: "numbing"/"numbs" as an **effect or efficacy claim** ("numbs the skin", "a numbing effect", "arriving already numbed", "numbing reduces sensation") remains banned everywhere, including these surfaces. Rationale: SEO/guide pages targeting the term are unreadable without the category noun, which names the product type, not an effect. `COMPLIANCE.md` updated same session. **NB — separate open P0:** the audit found live *effect-use* of "numbing/numbed" that contradicts the Hard Rule; per owner instruction that copy is **routed to MHG/legal review** and was **not changed** this session — it remains a pre-public-go-live gate.

### 2026-06-02 15:05 BST (Images — 1:1 square default, site-wide)
**Decision:** Standing rule (Canonical §11): **every image defaults to a 1:1 square aspect ratio** — `aspect-ratio: 1 / 1` on the wrapper + `object-fit: cover` on the img, so any source crops to square cleanly. Applied as the section DEFAULT (future pages inherit). Squared the non-square defaults: collection-hero (4/5 + 16/9 desktop), guide-hero (4/3), image-text-band (4/5 + 3/2 desktop), homepage hero stage (4/5), product-hero gallery (4/5), product-grid (4/5), product-showcase (removed desktop `aspect-ratio:auto`). Already-square sections (collection-grid, cross-sell, procedure-grid, trio-card-row, complete-prep) unchanged. **Non-square is allowed only as a deliberate per-section override, never the default.** Verified square across the 7 built collections + product pages + home (all media wrappers computed ratio 1.000); theme-check 0; Asset-API diff clean. Doc updated same session: BRAND.md (Images section).

### 2026-06-02 14:10 BST (Typography reweight — Strand 1: heads 700→400 + italic accent)
**Decision:** Site-wide heading reweight per the updated Design System (Strand 1). **Display/H1 + H2 drop from 700 → 400** (700 read "poster-like"); tracking → **-0.02em**; line-height **1.06/1.08** (H1), **1.1/1.12** (H2). **H3 / card titles + eyebrow unchanged (600).** New **italic accent = Montserrat italic 500** (`.ss-accent` / `.t-em`): exactly one emphasis word per hero/section head, same colour as the head (no purple), landing on the **Senseless-positioning/decision word, never the keyword** (so `numbing [format]` stays roman + crawlable; SEO/GEO unaffected). Implemented as global tokens in `senseless-typography.liquid` + reweighted every `senseless-*` section head (scoped classes hardcode their own weight) + a reusable `senseless-accent` snippet (`accent_word` setting on 8 head sections, empty-safe). The italic word-map (Notion `37358bc375ea81fbb88cefb276f24d19`) was applied **verbatim** to the 7 built collection pages (cream/gel/spray + 4 procedure); format-check, §5b, and FAQ heads stay clean. Verified cascade to home + product + section heads (all 400); theme-check 0; Asset-API diff clean. **Font preload note:** no explicit `<link rel=preload>` font list exists in the theme (Horizon loads via `font_face` + display:swap); 400 (body) + 500 (self-hosted medium) cover the heads; 700 retained only for the header wordmark, footer column labels, and the pull-quote glyph. **Docs updated same session:** CLAUDE.md + BRAND.md.

### 2026-06-01 23:10 BST (Wave 2 — all 10 product pages + §7 re-order + product-hero enhancements)
**Decision:** Re-ordered Clinical Cream to the Canonical State **§7 section order** (product-hero → **trust bar** → system band → **how-to-use** → key-facts → FAQ → reviews → related → aftercare; trust directly under hero, application below the system band) and **built the other 9 product pages** off that base template — each its own `templates/product.<handle>.json` + `templateSuffix`, generated from a shared section set (no new sections; no duplicates). **§7 product-page rules applied to all:** **no suitability matrix** (moved to collections, Wave 3); **size chips show size only**, single live price line; **variant-linked gallery image**; **Add to cart + Buy it now** (dynamic checkout); Judge.me reviews as an **app-block host** (renders nothing until install — launch-gate §9); injectable-clean; CPSR trust bar; no banned words; de-suffixed cross-links; live GBP prices from §1 (never £TBC); Product/Offer(OutOfStock)+BreadcrumbList+FAQPage schema. **product-hero adapted (site-wide):** size selector renders only when `variants.size > 1` (single-size products — Professional cream, all sprays, foam — show no selector); gallery image capped at `max-width:420px`; Buy-it-now + variant→image swap wired. **Default variant = larger size** (reordered: advanced-cream 30g, advanced/clinical/professional-gel 35ml; clinical-cream 30g already done; single-size products n/a). **Foam = tighter cleanser template** (no system band, no strength framing, "not a numbing product" FAQ, aftercare-pairing cross-sell — not a tier row). Per-product SEO meta composed compliant (the Master Page DB meta was banned-word stale: "everyday/concentration/flagship/clinics" — scrubbed). `theme check` 0 errors; all 10 rendered + verified on the preview theme.

### 2026-06-01 21:30 BST (Wave 2 — Product-page BASE TEMPLATE: Clinical Strength Cream)
**Decision:** Built `/products/clinical-strength-cream` as the **product-page base template** the other 9 products reuse, on the new store. Reconciled against `SECTIONS.md` — **reused** existing sections rather than building new: `senseless-product-hero` (S1 hero/buy box), `senseless-strength-matrix` (S2 suitability — its `suitability_row` block is exactly procedure×recommendation), `senseless-how-to-use` (S3), `senseless-decision-band` (S4 system band, 2 CTAs), `senseless-trust-bar` (S5), `senseless-key-facts` (GEO Key Facts), `senseless-faq-accordion` (S6, emits FAQPage), `senseless-cross-sell` (S8 related), `senseless-image-text-band` (S9 aftercare). **One NEW section:** `senseless-reviews` (Judge.me **app-block** host, `@app`; hidden until a block is added — Judge.me not yet installed on dev, flagged). Wired via `templates/product.clinical-strength-cream.json` + product `templateSuffix`. **Global-rule overrides applied** (injectable-clean suitability — dropped Lip Fillers/Botox rows; banned-word scrub — no everyday/concentration/flagship/clinical-grade; CPSR trust bar; Professional = 2px #6B3FA0 border + filled purple CTA, no badge word; de-suffixed cross-links). **Variant default = 30g** (reordered variants so 30g is position 1; live price £44.99/£19.99, never £TBC; sold-out state from inventory 0). SEO meta set on the product. Schema: Product+Offer (OutOfStock) + BreadcrumbList + FAQPage. **Two cross-cutting compliance fixes folded in (apply site-wide):** (1) **no neutral/black CTA borders** — `senseless-product-hero` add-to-cart now filled purple; `senseless-decision-band` + `senseless-strength-matrix` secondary CTAs moved black→purple; `senseless-cross-sell` related CTA is now a purple pill (filled for Professional). (2) **never-show-£TBC** — removed the `£TBC` price default from `cross-sell`, `product-showcase`, `product-grid`, `trio-card-row` (Shopify rejects empty text defaults → removed the default key entirely). `theme check` 0 errors; rendered + verified on the preview theme.

### 2026-06-01 17:10 BST (Stage B foundation fixes — new store senseless-numbing)
**Decision:** Four foundation fixes on the new `senseless-numbing` store / "Senseless Dev" theme `#199324434780` (unpublished; Horizon stays live). (1) **Montserrat weight 500 (`montserrat_n5`) now self-hosted** — emitted via `font_modify: 'weight','500'` + `font_face` in `senseless-typography.liquid`, giving the locked 400/500/600/700 set from the Shopify font CDN (still 0 Google Fonts). (2) **Brand purple wired into `scheme-1`** — `primary`, `primary_button_background/border` → `#6B3FA0`, hover → `#5A3489` (canonical darkened purple, matches the header's `--ss-purple-hover`); secondary button outline also moved to purple. Primary CTAs now render brand purple, not black. Canvas stays `#f7f7f5`. New `--brand-primary-hover` token documented in BRAND.md. (3) **Trust bar restored to the locked 4-signal set** — `UK formulated · Cosmetic product · CPSR assessed · Made for aesthetics`. Only `index.json` (homepage) + the section preset were short at 3 (missing "Cosmetic product"); all 14 other template instances already carried the 4-set. (4) **Bespoke `senseless-footer.liquid` built** — replaces Horizon native `footer` + `footer-utilities`. Dense, large inlined wordmark; four columns (Shop / The system / Brand / Newsletter) over an in-section legal band (© + Matrix Health Group Ltd attribution + `shop.policies` + social). Columns are menu-driven (`senseless-footer-shop/-explore/-company`); menus + policies don't exist on the fresh store yet, so an **injectable-clean placeholder set** renders per column and is flagged in-code for Stage D wiring. Default band **ink** (mirrors the prior scheme-5 dark footer; not a purple fill — respects "purple accent only"). Docs updated same session: `SECTIONS.md`, `ARCHITECTURE.md`, `BRAND.md`. `shopify theme check` 0 errors (24 pre-existing Horizon warnings). **Open:** Stage D must wire footer menus injectable-clean; confirm ink footer band vs canvas/surface.

### 2026-05-31 BST (Docs re-sync — ARCHITECTURE nav + COMPLIANCE "numbing" descriptor exception)
**Decision:** Two doc updates after the GEO/schema + quick-add builds merged to `dev`. (1) **`docs/ARCHITECTURE.md` re-synced to the live store nav** — the documented menu was stale (old per-procedure megamenu listing Lip Fillers/Botox). Reality is a **hub model** (`senseless-main`: Shop → By format / By procedure → hubs; The system; About; Help) and **injectable-clean** (Botox / Lip Fillers / Injections are not surfaced in nav; their collection templates still exist but are unlinked, under review for noindex/redirect). Footer documented to the live `senseless-footer-shop/-explore/-company` + MHG attribution band. (2) **`docs/COMPLIANCE.md` gains a narrow exception**: "numbing cream/gel/spray" is permitted as a **category/product-type noun** on the matching collection pages + their Key Facts (it names the category the page IS), while "numbing"/"numbs" as an **effect/efficacy claim** stays banned everywhere. This reconciles the audited v2 Key Facts copy (which uses the category noun) with the SEO-vs-body rule. Build sessions earlier today merged PR #1 (Phase-0), #2 (GEO/schema), #3 (quick-add) into `dev`; `theme check` 0 errors throughout.

### 2026-05-29 17:30 BST (Verification pass — CPSR confirmed; SECTIONS.md re-synced)
**Decision:** Owner **confirmed CPSR sign-off covers the Senseless range** (the prior 2026-05-29 15:00 entry shipped "CPSR assessed" on the assumption it was covered by the same certifications as Totally Numb; that assumption is now confirmed). The "CPSR assessed" trust signal therefore stays published in the default preset and across all 15 page instances as-is — no longer a conditional/flagged claim. Also: `docs/SECTIONS.md` re-synced from a stale 5-row index (2026-05-28) to the full 22-section + 3-snippet library with accurate per-template usage counts. Verified during a no-build verification session: Phases 0–3 were all already committed (`0b41e23`→`5df2e71`); Montserrat 400/700 preload already present in `snippets/fonts.liquid`; range guardrails clean (0 size-suffixed slugs, 0 Clinical-gel product refs, 0 customer-facing flagship/premium labels); `shopify theme check` 0 errors. Open item left unactioned by owner choice: footer payment-icon placeholder set (brief item, `footer-utilities` has no native payment-icons block).

### 2026-05-29 15:30 BST (Global build — Phase 3: card images)
**Decision:** Card images now resolve 1:1 from the linked object, not static uploads. New shared snippet `senseless-card-image.liquid` prefers a linked product's featured image / collection's image (then the collection's first product), with a graceful placeholder fallback; an `image_picker` remains as an override. Wired into the trio card row (tier/procedure/product/format cards), cross-sell, procedure grid and product showcase, all switched to `aspect-ratio:1/1` + `object-fit:cover`. The standalone `senseless-format-row` is left intentionally imageless (typographic-panel design from the variety pass). Images will populate automatically once product/collection media is uploaded and cards are linked. Commit `5df2e71`; theme check 0 errors; pushed to Senseless Dev #196680057167.

### 2026-05-29 15:00 BST (Global build — Phase 2: trust signal)
**Decision:** Added "CPSR assessed" to the trust bar across all 15 instances (already one per page). Canonical four-signal set is now **UK formulated · Cosmetic product · CPSR assessed · Made for aesthetics** — keeps both compliance anchors (cosmetic classification + CPSR safety assessment) and the brief's named signals, preserving the 4-up / 2×2 grid. "Cruelty-free" was dropped to keep four clean items (open to revisit). Same set seeded into the section preset. Commit `1137da6`.

### 2026-05-29 14:30 BST (Global build — Phase 1: header / nav / footer)
**Decision:** Configured + brand-styled Horizon's native header/footer rather than rebuilding, retaining native sticky/compress, accessible drawer, cart and search. Created **new** store nav menus (`senseless-main` header mega + `senseless-footer-shop` / `-explore` / `-company`) and pointed only the dev theme at them, leaving the shared `main-menu`/`footer` untouched so the live theme `195280437583` is unaffected. Header: text-column mega (Shop → By format / By procedure / Help choosing), The system + Help dropdowns, "Find your strength" CTA (href-targeted pill), accordion mobile drawer. Footer: 4 columns above the legal band. Created 13 backing pages from existing templates (faq, how-it-works, choosing-your-strength/format, how-to-apply, trade + SEO landing pages) and repointed `about-us` at the `about` template so all nav links resolve. Note: footer-utilities has no native payment-icons block (legal band = copyright + policies + social). Commit `ff791d9`.

### 2026-05-29 14:00 BST (Global build — Phase 0: typography supersedes Manrope/Inter)
**Decision:** Typeface is now **Montserrat**, self-hosted via the Shopify font CDN (zero `googleapis`/`gstatic` requests), wired through Horizon's font settings (body n4 / subheading n6 / heading n7 / accent n6). **Supersedes the 2026-05-27 Manrope + Inter decision.** Locked a colour-text ladder (`--text-primary #1A1816`, `--text-body #2B2730`, `--text-secondary #5C5853`, `--text-muted #8E8A82`, `--brand-primary #6B3FA0`, `--bg-canvas`, `--bg-surface`) and a fluid `clamp()` type scale in new global snippet `senseless-typography.liquid`; stripped the interim Google Fonts `@import` + remapped hexes across all 22 senseless sections. `docs/BRAND.md` + `CLAUDE.md` updated to match. Commit `0b41e23`.

### 2026-05-28 (MacBook Pro — range correction, build-side)
**Decision:** Reconciled the built theme to the corrected product range (Decisions Log 2026-05-28 22:40: 8 product pages, 10 SKUs; Clinical = cream only; no Clinical Gel; sizes are variants with size-agnostic slugs). Repo audit found the build had used the pre-correction range. Fixes across 23 files (commit `5c6dd28`): removed every Clinical Gel reference (Numbing Gel + Lip Fillers); de-suffixed all 45 size-suffixed product slugs to the 8 canonical forms; collapsed separate 10g/30g cream cards to one card per tier (Botox, Injections, Laser, Microneedling, SPMU, Waxing, Numbing Cream); Numbing Gel → 2 tier cards + 2-SKU grid; Lip Fillers grid → Clinical Cream / Advanced Cream / Advanced Gel; gel "three strengths" → two and "four formats" → three formats; cleared leftover Professional "Flagship" badges. theme check 0 errors; pushed to Senseless Dev #196680057167 only. Notion: Clinical Gel + two merged 10g cream rows already archived (left so); Numbing Gel + Lip Fillers collection rows → QA; 8 canonical product rows left In Progress (template built, Shopify products/variants/tags pending admin). Build report posted to Build Reports.

### 2026-05-28 (MacBook Pro — full site build session)
**Decision:** Built and deployed Batches 2–6 to Senseless Dev `#196680057167` (0 theme-check errors, clean push). 22 page/collection templates + 9 new `senseless-` sections (procedure-grid, product-hero, strength-matrix, how-to-use, cross-sell, guide-hero, rich-text, callout-band, contact-form). Key decisions: (1) **Products modelled as 9 logical SKUs via ONE shared `product.json` with a native variant picker** (Clinical/Advanced cream = size variants), not 11 templates — Notion DB still lists 11 separate product rows; the two creams should be single products with 30g/10g variants in admin. (2) **Spray slugs canonicalised to `/products/advanced-strength-spray` + `/products/professional-strength-spray`** (DB canonical) rather than the `-100ml` form some briefs used, to avoid dead links. (3) **Landing pages built = the 4 actually Briefed in Notion** (Senseless vs Ametop, Strongest, Best Numbing Cream, Best Emla Alternative UK) — the launch brief's names differed. (4) **Professional tier treatment on product pages is driven by product tag `Professional` / metafield `senseless.tier`** so the shared template self-applies the flagship styling. (5) **`shopify theme check` does NOT catch `default:""` on text settings** — server-side `theme push` rejects it ("default can't be blank"); fixed on product-hero. Product rows left at **In Progress** (templates built; products + variants + tier tags need creating in Shopify admin); all 22 non-product pages set to **QA**. Build report posted to Notion Build Reports. Policy pages + blog cluster skipped (Brief Status = Not Started).

### 2026-05-28 14:30 BST
**Decision:** Homepage built — 5 reusable sections (`senseless-hero-brand-led`, `senseless-trio-card-row`, `senseless-image-text-band`, `senseless-trust-bar`, `senseless-newsletter-signup`) + `templates/index.json` wiring all 8 homepage instances, copy populated verbatim from the audited Notion brief, all editable via theme editor. **Critical correction — dev theme target:** the Shopify CLI's default store is `matrix-group-totally-numb` (Totally Numb), and theme `#193366131072` belonged to that store, not Senseless. A first push accidentally landed there. Created a new unpublished theme **"Senseless Dev" `#196680057167`** on `senseless-tattooing.myshopify.com` — now the canonical Senseless dev theme. `.env` `SHOPIFY_DEV_THEME_ID` updated to `196680057167`; CLAUDE.md hard rule 11 updated to always pass `--store senseless-tattooing.myshopify.com`.

### 2026-05-28 13:45 BST
**Decision:** Three bootstrap open items resolved. (1) Dangling symlink `.cursor/skills/accessibility` (pointed to a non-existent `.claude/skills/accessibility` target that never came across in the clean Horizon build) deleted. (2) Theme-deploy ownership fixed: theme deploys go through Shopify CLI only (`shopify theme push --theme $SHOPIFY_DEV_THEME_ID`); the client_credentials API token (`shpca_`) cannot see CLI dev themes and is used only for products, collections, metafields, files, and content. (3) Branching strategy adopted: all day-to-day build work happens on `dev`; `main` is stable and merged into only at sprint/milestone completion; never commit directly to `main` during build sessions. `/session-start` checks out `dev` and pulls; `/session-end` commits and pushes to `dev`. Encoded in CLAUDE.md (hard rules 11–12 + Branching Strategy section) and the session-start/session-end command docs.

### 2026-05-27 09:00 BST
**Decision:** Mac mini directory wiped, GitHub repo history wiped, fresh clone of latest Horizon theme initialised, full project scaffolding created (10 skills, 4 slash commands, 4 docs, CLAUDE.md, image pipeline, .env.example, .gitignore, README, manifest). First clean commit pushed to GitHub.

### 2026-05-27 08:45 BST
**Decision:** Three-machine protocol locked in. Every CC session must begin with machine identity check. Git is the synchronisation layer between Mac mini and MacBook Pro. Local DECISIONS-LOG.md must be pulled fresh at session start.

### 2026-05-27 08:40 BST
**Decision:** Existing senseless-theme directory wiped on Mac mini. GitHub history wiped (Option B — kept repo URL). Fresh build starts from latest Horizon as a clean base.

### 2026-05-27 08:30 BST
**Decision:** Decisions Log restructured into paginated volume system in Notion. Sub-pages named "Vol N — [month] [year]". When a volume fills, a new one is created; old volumes never deleted. Local DECISIONS-LOG.md mirrors active volume only. Six-layer three-way sync protocol documented. Skills raised to 10 (added drift-check). Slash commands raised to 4 (added /drift-check).

### 2026-05-27 08:00 BST
**Decision:** Final Claude Code workflow structure locked in. 10 skills, 4 slash commands, 4 docs, 1 CLAUDE.md, image pipeline scaffolding.

### 2026-05-27 07:45 BST
**Decision:** Image Management System ported from Totally Numb. Pipeline: Sharp compression → Shopify Files staged upload → alt text set → manifest entry. Senseless naming prefix.

### 2026-05-27 07:30 BST
**Decision:** Typography confirmed. Manrope (500–700) headings, Inter (400) body. Reference brands Augustinus Bader, Dieux, Wildsmith Skin, 111SKIN. Clean geometric sans.

### 2026-05-27 07:15 BST
**Decision:** New theme built from latest Horizon as clean start. Brand purple `#6B3FA0`.

### 2026-05-27 07:00 BST
**Decision:** UK compliance non-negotiable. See docs/COMPLIANCE.md.

### 2026-05-27 06:55 BST
**Decision:** Senseless is a UK cosmetic product, not a medicine. Avoids MHRA borderline threshold. "Numbing" only in SEO/URLs/meta, never in claims about product effect.

### 2026-05-27 06:50 BST
**Decision:** Lidocaine-free positioning approved. Eugenol-based active.

### 2026-05-26 22:00 BST
**Decision:** Tier hierarchy: Clinical < Advanced < Professional. No percentage claims.

### 2026-05-26 21:55 BST
**Decision:** 10 SKUs confirmed. S30CL, S30AD, S30PR / S10CL, S10AD / SG35CL, SG35AD, SG35PR / SSPAD, SSPPR.

---

*For the full history, see the Notion Decisions Log: https://www.notion.so/36c58bc375ea81fd8a68c983b41c8a86*
